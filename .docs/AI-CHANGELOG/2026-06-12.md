# [Dev Team] Changelog: 2026-06-12

🛡️ สรุปรายงานการแก้ไขข้อบกพร่องและปิดช่องโหว่ความสอดคล้องสถานะทั้งหมด 100% จากรายงาน [state_sync_audit.md](file:///c:/Users/ASUS/Desktop/Project/Delivery/state_sync_audit.md) ที่ไม่ได้ระบุในวันก่อนหน้า

---

## 🏛️ 1. API & SignalR Contracts (BUG-01 ถึง BUG-06)
* **BUG-01 & BUG-03 (OrderStatusChanged Mismatch):** ปรับให้ Backend ส่ง JSON object แทน positional args และอัปเดต client ทุกตัวให้รองรับการแกะข้อมูลแบบ object มี backward compatibility ครบถ้วน
* **BUG-02 (Order DTO Status Default):** ปรับค่าเริ่มต้นของสถานะสั่งซื้อจาก `"PENDING"` เป็น `"CREATED"` ใน C# DTO และโมเดลฝั่ง Flutter ให้ตรงตามหลัก State Machine
* **BUG-04 & BUG-05 (Rider State & UpdateStatus Document):** แก้ไขเอกสารระบุการเปลี่ยนสถานะของไรเดอร์เมื่อตอบรับงานเป็น `RESERVED` -> `BUSY` และระบุสถานะไรเดอร์ในระบบทั้งหมดอย่างถูกต้อง
* **BUG-06 (Customer Order Status Broadcast):** พัฒนาบริการ `customer_signalr_service.dart` ในแอปมือถือลูกค้า ให้ดักรับเหตุการณ์การเปลี่ยนสถานะออเดอร์เรียลไทม์

---

## 📱 2. Mobile App & Backend Integration (BUG-07 ถึง BUG-11)
* **BUG-07 & BUG-08 (Location Update & Accuracy Semantics):** พัฒนาฟังก์ชัน `sendLocationUpdate` ใน Flutter ให้รองรับการทำงาน และปรับเปลี่ยนการส่งค่า semantic จากส่ง speed เป็นส่ง `accuracy` ตามข้อกำหนด API ของระบบ telemetry หลักหลังบ้าน
* **BUG-09 (DTO Inconsistency):** ซิงค์ฟิลด์ `items`, `shopId`, และตัวแปรกลุ่มงานพ่วง (batch group details) ในโครงสร้างโมเดล DTO ทั้งของ Angular และ Flutter ให้แสดงผลตรงกัน
* **BUG-10 (Dangerous Feature Removed):** ลบฟีเจอร์และปุ่ม `deleteAllData()` / เอนด์พอยต์ `DELETE /orders/all` ทั้งระบบเพื่อความปลอดภัยระดับฐานข้อมูล (Security-by-Design)
* **BUG-11 (Generic Search in Base Controller):** พัฒนาลอจิก ApplySearch ใน `CrudControllerBase` โดยใช้ Dynamic Expression Trees และ Reflection ในการดึงและคัดกรองข้อมูลฟิลด์ที่เป็น string อัตโนมัติ

---

## 🛰️ 3. Multi-Drop & Stale State Sync Gaps (BUG-12 ถึง BUG-19)
* **BUG-12 (Multi-Drop Coords Broadcast):** พัฒนาคลาส `ActiveOrderRecipientCache` จัดเก็บรูปแบบ Hash ใน Redis และใช้สคริปต์ Lua อัปเดตแบบ Atomic เพื่อให้ลูปกระจายพิกัด GPS ของไรเดอร์ให้ทุกลูกค้าที่อยู่ในรอบงานพ่วงเดียวกัน (ไม่ถูกจำกัดไว้แค่ลูกค้ารายสุดท้าย)
* **BUG-13 (Cache Stale & Fallback):** ลดเวลาหมดอายุแคชแอคทีฟ (TTL) เหลือ 30 วินาที พร้อมเพิ่ม PostgreSQL query fallback เมื่อแคชตกหล่น ป้องกันพิกัดไรเดอร์รั่วไหลไปหาลูกค้าคนเดิมหลังจบงาน
* **BUG-14 & BUG-15 (Client Reset & Coordinates Null-Safety):** เพิ่มการรีเซ็ตพิกัดและล้าง Marker บนแผนที่ทันทีเมื่อ watch order หรือสลับออเดอร์ใหม่ พร้อมเพิ่ม null safety เช็คพิกัดของร้านค้า/ลูกค้า ป้องกันแผนที่บนโมบายแครช
* **BUG-16 (Telemetry Double-Counting):** ปรับเงื่อนไขคัดกรองใน `TelemetryBroadcastWorker` ไม่ให้นับไรเดอร์สถานะ `STALE` เข้าไปซ้ำซ้อนในจำนวน Active Riders
* **BUG-17 & BUG-18 (Missing SignalR Broadcasts):** เพิ่มการยิง SignalR อัปเดตสถานะออเดอร์กลับเป็น `MATCHING` เมื่อข้อเสนอถูกปฏิเสธหรือหมดเวลา (ใน `DispatchOfferHandler`) และบรอดแคสต์สถานะ `STALE`/`OFFLINE` หาแอดมินเมื่อไรเดอร์ขาดการส่งสัญญาณชีพ (ใน `HeartbeatMonitor`)
* **BUG-19 (Batch Cancellation UI/UX):** แสดง Dropdown ให้แอดมินเลือกยกเลิกออเดอร์แบบเจาะจงหรือยกเลิกทั้งหมดในรอบพ่วงได้ผ่านแผนที่ และใช้ `forkJoin` ส่งคำขอยกเลิกแบบขนานข้ามเครือข่าย

---

## 🔒 4. Cancellation Rules & ETA (CANCEL-01/02, ETA-01/02)
* **CANCEL-01 (DELIVERING → CANCELLED):** เพิ่มเงื่อนไขอนุญาตให้ยกเลิกสถานะกำลังจัดส่งใน `OrderStateRules` สำหรับกรณีฉุกเฉิน
* **CANCEL-02 (Redis Offer Lock Release):** คืนสิทธิ์การจองคิวมอบหมายงานและปลดล็อกข้อเสนอใน Redis ทันทีเมื่อออเดอร์ถูกยกเลิก
* **ETA-01 & ETA-02 (Dynamic Factors & Fallback ETA):** ดึงปัจจัยอากาศ/จราจรจริงจาก config วิเคราะห์ Rush Hour อัตโนมัติ และปรับ fallback calculation ของ backend ให้คำนวณบวกเวลา pickup (10 นาที) / dropoff (3 นาที) ตรงกันกับ AI Engine

---

## 🚀 5. Gaps & Bugs Resolved Today (BUG-20 ถึง BUG-22, GAP-01 ถึง GAP-03)
* **BUG-20 (Rider State Sync on Connect):** เพิ่มกลไกบรอดแคสต์ SignalR `RiderStatusUpdated` และ `RiderLocationUpdated` ทันทีหลังขยับสถานะสำเร็จใน Event Handler เพื่อให้หน้า Admin Dashboard แสดงสถานะคนขับออนไลน์เป็น `IDLE`/`BUSY` ทันที ไม่ค้าง `OFFLINE`
* **BUG-21 (Test Prefix Fix):** แก้ไขเส้นทาง URL endpoints ทั้ง 9 รายการใน [TelemetryControllerTests.cs](file:///c:/Users/ASUS/Desktop/Project/Delivery/RootScripts/scripts.test/BackendApi.IntegrationTests/Telemetry/TelemetryControllerTests.cs) ให้มี `/v1/` นำหน้าเพื่อแก้ไขข้อผิดพลาด HTTP 404
* **BUG-22 (Angular Speed Mapping):** ใส่การแมปค่าฟิลด์ความเร็ว `speedKmh` จากข้อความ SignalR ใน [tracking-signalr.service.ts](file:///c:/Users/ASUS/Desktop/Project/Delivery/admin-dashboard/src/app/core/services/tracking-signalr.service.ts) แก้ไขค่าความเร็วแสดงผลเป็น 0 km/h ค้างตลอดเวลา
* **GAP-01 (Customer Map Tracking Coords):** พัฒนาตัวรับสัญญาณและฟังก์ชันวาดหมุด GPS ของพนักงานจัดส่งแบบเรียลไทม์บนแอปพลิเคชันลูกค้า
* **GAP-02 (Connection Reconnection Sync):** เพิ่มการดึงข้อมูลทวนซ้ำ (fetch latest state) เมื่อระบบคืนการเชื่อมต่อพร้อมสุ่มหน่วงเวลา Jitter เพื่อป้องกันปัญหา Thundering Herd
* **GAP-03 (Redis Key Leak):** สั่งลบข้อมูลคีย์ความเร็วบัฟเฟอร์ `riders:speed_buffer:{riderId}` ทันทีเมื่อไรเดอร์ออฟไลน์ใน `RemoveRiderAsync` ป้องกัน memory leak ใน Redis

---

## 🔬 6. ผลการทดสอบความถูกต้องภาพรวมล่าสุด (Comprehensive Testing Outcomes)
* 🟢 **C# Unit Tests:** 100/100 Passed (ผ่านทั้งหมด 100%)
* 🟢 **C# Integration Tests:** 54/54 Passed (ผ่านทั้งหมด 100% รวมถึงการทดสอบ Telemetry ทั้ง 9 รายการ)
