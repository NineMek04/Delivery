# AI-Generated Changelog: 2026-06-02

## รายสรุปภาพรวม (Summary)
วันนี้เราได้ทำการยกระดับเสถียรภาพและคุณภาพความพร้อมใช้งาน (Hardening & Integration) ของแอปพลิเคชันไรเดอร์ (Rider Mobile App) เพื่อแก้ปัญหาด้านกระบวนการทำงานออฟไลน์ (Offline Capabilities), ความปลอดภัยในการยืนยันความคืบหน้าของออเดอร์, และการชนกันของคำขอเปลี่ยนโทเค็นหมดอายุ (Concurrent Token Refresh Race Condition)

---

## 1. การบูรณาการหน้ายืนยันจัดส่งสำเร็จ (Delivery Confirmation Screen Integration)
ปรับปรุงขั้นตอนสุดท้ายของการส่งงาน (Order Completion) ให้รัดกุมด้วยหลักฐานการส่งมอบ:
*   **GoRouter Registration**: ลงทะเบียนเส้นทางนำทาง (Route) ใหม่ `/delivery/confirm/:orderId` เข้าไปใน Rider ShellRoute ใน [app_router.dart](file:///d:/Delivery/rider_app/lib/app/app_router.dart) พร้อมปรับเปลี่ยนตัวแปร `_calculateSelectedIndex` เพื่อสแตนด์บาย Bottom Navigation Bar ให้อยู่ในแท็บ **"งานส่ง" (Index 1)** ตลอดเวลาที่อยู่ในหน้านี้
*   **Redirect Completion Button**: ปรับแก้ใน [active_delivery_screen.dart](file:///d:/Delivery/rider_app/lib/features/delivery/screens/active_delivery_screen.dart) โดยหากสเตตัสงานที่จะอัปเดตเป็นขั้นตอนสุดท้าย (`COMPLETED`) ระบบจะผลักหน้าจอนำทาง (Push) ไปยังหน้า [delivery_confirmation_screen.dart](file:///d:/Delivery/rider_app/lib/features/delivery/screens/delivery_confirmation_screen.dart) แทนการส่งคำขออัปเดตตรงๆ
*   **Riverpod State Connection**: ปรับปรุงเมธอด `_submit()` ใน [delivery_confirmation_screen.dart](file:///d:/Delivery/rider_app/lib/features/delivery/screens/delivery_confirmation_screen.dart) ให้เรียกใช้ `deliveryNotifierProvider.notifier.updateOrderStatus(...)` จริง เพื่อปลดจบสถานะงาน และควบคุมการนำทางกลับหน้าแรก (`home`) เมื่อทำรายการเสร็จสิ้น

---

## 2. ระบบทำงานออฟไลน์สำหรับสเตตัสงาน (Offline Action Queue)
ป้องกันปัญหาคนขับอาหารอยู่ในจุดอับสัญญาณแล้วกดอัปเดตสถานะงานไม่ได้:
*   **SQLite Database Schema Upgrade (v2)**: เพิ่มฟังก์ชัน `onUpgrade` และ `onCreate` ใน [local_database_service.dart](file:///d:/Delivery/rider_app/lib/core/database/local_database_service.dart) เพื่อยกระดับฐานข้อมูล SQLite เป็น `version: 2` และรองรับตารางใหม่ `pending_status_updates` (พร้อมโครงสร้าง List memory-fallback ในระดับ Web Platforms)
*   **Dio Error Interception & Optimistic Update**: ปรับปรุงฟังก์ชัน `updateOrderStatus` ใน [delivery_provider.dart](file:///d:/Delivery/rider_app/lib/features/delivery/providers/delivery_provider.dart) ให้ดักจับ Dio Connection Errors (Timeout, Connection Error) หากตรวจสอบพบว่าอุปกรณ์ออฟไลน์อยู่ ระบบจะจัดเก็บคิวงานลงเครื่อง และสลับไปทำ **Optimistic State Transition** ทันที เพื่อปรับเปลี่ยน UI เสมือนว่าทำสำเร็จแล้วเพื่อความลื่นไหลของไรเดอร์
*   **Background Sync Scheduler**: เพิ่มฟังก์ชัน `syncPendingStatusUpdates()` ใน [gps_buffer_service.dart](file:///d:/Delivery/rider_app/lib/core/location/gps_buffer_service.dart) ดึงคิวงานจากตาราง SQLite ออกมาทยอยล้างคำขออัปเดตสเตตัสขึ้นหลังบ้านแบบเรียงลำดับเวลา (FIFO) โดยตัวซิงค์จะทำงานร่วมกันกับ Periodic Sync Timer ของพิกัด GPS ทุก 5-15 วินาที
*   **UX Banner Integration**: ปรับปรุง [active_delivery_screen.dart](file:///d:/Delivery/rider_app/lib/features/delivery/screens/active_delivery_screen.dart) เพื่อแยกแยะข้อความแจ้งเตือนประเภทออฟไลน์ (`Offline: บันทึกสถานะงานแบบออฟไลน์แล้ว`) ให้ออกมาเป็นแถบ SnackBar สีเขียวที่แจ้งเตือนคนขับอย่างสวยงามแทนการเด้ง Error Dialog สีแดงขัดจังหวะ

---

## 3. ป้องกันการขอ Token ซ้ำซ้อนระดับเครือข่าย (Concurrent Token Refresh Guard)
แก้ปัญหาคำขอ HTTP ยิงเบียดแย่งชิงสิทธิ์เปลี่ยนโทเค็นจนสุ่มสไลด์ผู้ใช้กลับหน้าล็อกอิน (Force Logout):
*   **Request Queueing**: ปรับเปลี่ยน `ErrorInterceptor` ใน [api_interceptors.dart](file:///d:/Delivery/rider_app/lib/core/api/api_interceptors.dart) ให้จดจำคิวและแช่แข็งคำขอ API ลำดับถัดไปที่พบ Error 401 เอาไว้ชั่วคราวผ่าน List queue ในกรณีที่ระบบกำลังทำทรานแซกชัน `/api/v1/auth/refresh`
*   **Concurrent Flusher**: เมื่อรีเฟรช Token สำเร็จ คำขอทั้งหมดที่ถูกแช่แข็งอยู่ในคิวจะถูกปล่อยออกไปอีกครั้งพร้อมๆ กันโดยอัตโนมัติพร้อมแนบ Token Bearer ชุดใหม่ หากรีเฟรชล้มเหลว คิวงานทั้งหมดจะถูกปฏิเสธ (Reject) และนำผู้ใช้กลับหน้า Login อย่างปลอดภัย

---

**ผู้บันทึก:** Antigravity AI
